<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/LinearLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="22115053122127_Phan Minh Nhat"
        android:textSize="32sp"/>

    <!-- Nút Show All Contact cũ -->
    <Button
        android:id="@+id/btnshowallcontact"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Message" />

    <!-- Nút Show Call Log -->
    <Button
        android:id="@+id/btnshowcalllog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Call Log" />

    <!-- Nút Show All Contact 2 mới -->
    <Button
        android:id="@+id/btnshowallcontact2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show All Contact 2" />

</LinearLayout>
